<!-- filepath: server/src/templates/emails/test-email.hbs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; background-color: #f4f4f4; }
        .container { max-width: 600px; margin: 0 auto; background-color: #ffffff; }
        .header { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 40px 20px; text-align: center; }
        .header h1 { color: #ffffff; font-size: 24px; margin: 0; }
        .content { padding: 40px 30px; }
        .info-box { background-color: #e7f3ff; border-left: 4px solid #0084ff; padding: 15px; margin: 20px 0; }
        .success-icon { font-size: 64px; text-align: center; margin: 20px 0; }
        .footer { background-color: #f8f9fa; padding: 30px; text-align: center; color: #6c757d; font-size: 14px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{appName}}</h1>
        </div>
        <div class="content">
            <div class="success-icon">✅</div>
            <h2 style="color: #28a745; text-align: center; margin-bottom: 20px;">Email Service is Working!</h2>
            <p style="text-align: center;">This is a test email to verify your email configuration.</p>
            
            <div class="info-box">
                <p><strong>Test Time:</strong> {{testTime}}</p>
                <p><strong>Mail Host:</strong> {{mailHost}}</p>
                <p><strong>Mail User:</strong> {{mailUser}}</p>
            </div>

            <p style="margin-top: 30px; text-align: center; color: #6c757d;">
                If you're seeing this email, your email service is configured correctly! 🎉
            </p>
        </div>
        <div class="footer">
            <p>&copy; 2024 {{appName}}. All rights reserved.</p>
        </div>
    </div>
</body>
</html>